name: Verify Revenue W2-D AC Projection

on:
  pull_request:
    paths:
      - 'revenue/activecampaign_projection.py'
      - 'tests/test_activecampaign_projection.py'
      - '.github/workflows/revenue-w2d-ac-projection-verify.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --disable-pip-version-check pytest
      - run: python -m py_compile revenue/activecampaign_projection.py
      - run: python -m pytest -q tests/test_activecampaign_projection.py
