name: Apply Campaign Readiness Patch

on:
  push:
    branches:
      - 'fix/campaign-readiness-20260831'

permissions:
  contents: write

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke
        run: echo campaign-readiness-helper-ok
