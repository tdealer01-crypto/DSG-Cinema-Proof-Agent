{
  manifest_version: "0.1.0",

  config_schema: {
    type: "object",
    properties: {
      openai_api_key: { type: "string", secret: true },
      openai_model: { type: "string", default: "gpt-5" },
      reasoning_effort: {
        type: "string",
        enum: ["low", "medium", "high"],
        default: "medium",
      },
      cinema_preflight_mode: {
        type: "string",
        enum: ["off", "required"],
        default: "off",
      },
      cinema_plan_id: { type: "string", default: "" },
      cinema_plan_bindings_json: { type: "string", default: "" },
      cinema_agent_identity: { type: "string", default: "" },
      cinema_api_key: { type: "string", secret: true, default: "" },
    },
    required: ["openai_api_key"],
    additionalProperties: false,
  },

  slots: {},

  program: {
    image: "ghcr.io/tdealer01-crypto/dsg-pibench-agent:latest",
    entrypoint: [
      "python",
      "/app/server.py",
      "--host",
      "0.0.0.0",
      "--port",
      "9010"
    ],
    env: {
      OPENAI_API_KEY: "${config.openai_api_key}",
      OPENAI_MODEL: "${config.openai_model}",
      REASONING_EFFORT: "${config.reasoning_effort}",
      CINEMA_PREFLIGHT_MODE: "${config.cinema_preflight_mode}",
      CINEMA_PIBENCH_PLAN_ID: "${config.cinema_plan_id}",
      CINEMA_PIBENCH_PLAN_BINDINGS_JSON: "${config.cinema_plan_bindings_json}",
      CINEMA_PIBENCH_AGENT_IDENTITY: "${config.cinema_agent_identity}",
      CINEMA_API_KEY: "${config.cinema_api_key}",
    },
    network: {
      endpoints: [
        { name: "a2a_endpoint", port: 9010 },
      ],
    },
  },

  provides: {
    a2a: { kind: "a2a", endpoint: "a2a_endpoint" },
  },

  exports: { a2a: "a2a" },
}
