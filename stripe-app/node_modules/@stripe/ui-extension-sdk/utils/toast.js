import { getHostEndpoint } from './_endpoint';
/** @public */
export const showToast = async (message, options = {}) => {
    const endpoint = getHostEndpoint();
    return endpoint.call.showToast(message, options);
};
