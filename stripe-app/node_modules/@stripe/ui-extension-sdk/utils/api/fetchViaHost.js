import { getHostEndpoint } from '../_endpoint';
export const fetchViaHost = async (encodedUrl, options = {}) => {
    const url = new URL(encodedUrl);
    return getHostEndpoint().call.stripeApiFetch(url.pathname + url.search, options);
};
