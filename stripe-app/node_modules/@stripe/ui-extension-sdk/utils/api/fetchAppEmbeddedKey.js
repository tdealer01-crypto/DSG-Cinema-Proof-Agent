import { getHostEndpoint } from '../_endpoint';
export const fetchAppEmbeddedKey = async () => {
    const apiKey = await getHostEndpoint().call.fetchAppEmbeddedKey();
    if (!apiKey) {
        throw new Error('Unable to fetch app embedded key');
    }
    return apiKey;
};
