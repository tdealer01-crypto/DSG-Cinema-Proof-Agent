import { getHostEndpoint } from '../_endpoint';
export const supportsFetchAppEmbeddedKey = async () => getHostEndpoint()
    .call.supportsFetchAppEmbeddedKey()
    .then((supported) => supported)
    .catch(() => false);
