import type { NestableJSONValue } from '../types/util';
/**
 * Fetches a cryptographic signature for use with Stripe's App Embedded Backend.
 *
 * @param additionalPayload - Optional JSON-serializable payload to include in the signature request.
 * @returns A promise resolving to the signature string.
 * @public
 */
export type FetchStripeSignature = (additionalPayload?: NestableJSONValue) => Promise<string>;
/** @public */
export declare const fetchStripeSignature: FetchStripeSignature;
