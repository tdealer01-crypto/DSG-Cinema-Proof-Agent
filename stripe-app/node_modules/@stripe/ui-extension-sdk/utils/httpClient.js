/* eslint-disable max-classes-per-file */
import invariant from 'invariant';
import { stripeApiFetch } from './apiFetch';
const matchesStripeKey = /[ps]k_(test|live)_[A-Za-z0-9]+/;
/** @internal */
class StripeAppsHttpResponse {
    constructor(resp) {
        this._resp = resp;
    }
    getHeaders() {
        return this._resp.headers;
    }
    getStatusCode() {
        return this._resp.status;
    }
    getRawResponse() {
        return this._resp;
    }
    // eslint-disable-next-line class-methods-use-this
    toStream() {
        throw new Error('Streams have not been implemented in the Stripe HTTP client');
    }
    // eslint-disable-next-line @typescript-eslint/no-wrapper-object-types
    toJSON() {
        const { json } = this._resp;
        if (json === undefined) {
            return Promise.reject(new Error('Response body undefined'));
        }
        else {
            return Promise.resolve(json);
        }
    }
}
/**
 * An HTTP client compatible with `stripe-node` that routes API requests through the Dashboard session.
 * @public
 */
export class StripeAppsHttpClient {
    constructor(fetch) {
        this._fetch = fetch;
    }
    // eslint-disable-next-line class-methods-use-this
    getClientName() {
        return 'stripe-ui-extension';
    }
    async makeRequest(host, port, path, method, headers, requestData, protocol, _timeout) {
        invariant(protocol === 'https', 'Must use https connections in UI extensions');
        const fetchOptions = {
            method,
            headers,
        };
        if (requestData) {
            fetchOptions.body = requestData;
        }
        const authHeader = headers.Authorization;
        if (authHeader && matchesStripeKey.test(authHeader)) {
            throw new Error('Do not use actual stripe keys when using the Stripe JS API client with UI extesions.\n\n Instead, use `STRIPE_API_KEY` from `@stripe/ui-extension-sdk/http_client` as a placeholder.');
        }
        const url = new URL(path, `${protocol}://${host}`);
        const resp = await this._fetch(url.toString(), fetchOptions);
        // TODO: Add support for timeouts.
        return new StripeAppsHttpResponse(resp);
    }
}
/**
 * A placeholder API key to pass to `stripe-node` when using the extension HTTP client.
 * The actual authentication is handled by the Dashboard session.
 *
 * **Do NOT change this string** without a deprecation plan — the Dashboard runtime validates
 * this exact value and will throw if it differs.
 * @see manage/frontend/src/tailor/extensions/host/api_fetch.js
 * @public
 */
export const STRIPE_API_KEY = 'DO_NOT_PASS_A_REAL_API_KEY';
/**
 * Creates a pre-configured {@link StripeAppsHttpClient} instance using the default API fetch.
 *
 * @returns A new `StripeAppsHttpClient` instance.
 * @public
 */
export const createHttpClient = () => new StripeAppsHttpClient(stripeApiFetch);
/**
 * The standard HTTP header name used for authorization.
 * @public
 */
export const AUTHORIZATION_HEADER = 'Authorization';
/**
 * The pre-built authorization header value using {@link STRIPE_API_KEY}.
 * @public
 */
export const AUTHORIZATION_VALUE = `Bearer ${STRIPE_API_KEY}`;
