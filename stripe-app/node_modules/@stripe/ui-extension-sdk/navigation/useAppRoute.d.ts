import type { RouteKey, RouteParams } from './types';
type CurrentRoute = {
    [K in RouteKey]: {
        key: K;
        routeParams: RouteParams<K>;
    };
}[RouteKey];
/**
 * Returns the current app route.
 *
 * When you check route name, TypeScript narrows route parameters
 * to the exact shape declared by that route's URL pattern.
 *
 * Requires an AppRouter higher up in the React tree.
 *
 * @see https://docs.stripe.com/stripe-apps/routing
 */
export declare function useAppRoute(): CurrentRoute;
export {};
