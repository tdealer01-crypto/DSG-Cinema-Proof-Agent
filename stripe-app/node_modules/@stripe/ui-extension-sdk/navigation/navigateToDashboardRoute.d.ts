import { RouteDescriptor } from '../ui';
/**
 * Navigates users to specific Dashboard page defined by a route descriptor
 * without triggering a full-page reload.
 *
 * @public
 * @see https://docs.stripe.com/stripe-apps/route-descriptors
 */
export declare function navigateToDashboardRoute(descriptor: RouteDescriptor): Promise<void> | undefined;
