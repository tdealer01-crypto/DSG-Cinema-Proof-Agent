import { useContext } from 'react';
import { RoutesConfigContext } from './RoutesConfigContext';
export function useRoutesConfig() {
    const config = useContext(RoutesConfigContext);
    if (!config) {
        throw new Error('NavigationProvider is not initialised.');
    }
    return config;
}
