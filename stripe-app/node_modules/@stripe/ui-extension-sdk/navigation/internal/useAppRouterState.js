import { useContext } from 'react';
import { AppRouterContext } from './AppRouterContext';
export function useAppRouterState() {
    const state = useContext(AppRouterContext);
    if (!state) {
        throw new Error('AppRouter is not initialised.');
    }
    return state;
}
