export function getExtRouter() {
    const router = globalThis.__extRouter;
    if (!router) {
        throw new Error('ExtensionRouter is not found. It should be injected during sandbox initialisation.');
    }
    return router;
}
