import { RouteDescriptor, RouteKey } from './types';
import { ExtensionContextValue } from '../utils';
export type AppRouterProps = {
    /**
     * The context object provided to UI Extensions containing information about
     * the current user, environment, and app.
     */
    context: ExtensionContextValue;
    /**
     * Specifies the route to navigate to when the current URL doesn’t match
     * any defined route. The navigation uses history replacement so
     * the unmatched URL won’t appear in the browser’s back stack.
     */
    redirectOnNotFound?: RouteDescriptor<RouteKey>;
};
/**
 * Renders the view defined by the `route` entry whose path pattern matches
 * the current URL.
 *
 * Place it in your component implementing the `stripe.dashboard.fullpage` viewport.
 *
 * Requires a NavigationProvider higher up in the React tree.
 *
 * @see https://docs.stripe.com/stripe-apps/routing
 */
export declare function AppRouter({ context, redirectOnNotFound }: AppRouterProps): import("react/jsx-runtime").JSX.Element | null;
