import * as React from 'react';
import { RemoteComponentType } from '@remote-ui/core';
export type ListPageColumn = {
    /** The unique identifier for the column. */
    key: string;
    /** The label to display for the column. */
    label: string;
    /** Sets the column label to be hidden. */
    hideHeaderLabel?: boolean | undefined;
    /** Cell format options. */
    cell?: ListPageCell | undefined;
    /** Whether the column is pinned. */
    pinned?: boolean | undefined;
};
export type ListPageCell = TextTableCell | LinkTableCell | DateTableCell | IdTableCell | StatusTableCell | CurrencyTableCell;
type TextTableCell = {
    /** The type of the cell. */
    type: 'text';
};
type LinkTableCell = {
    /** The type of the cell. */
    type: 'link';
    /** A map of cell values (URLs) to human-readable display text for links. Each key is a URL that may appear as a cell value, and its corresponding value is the text to display for that link. If a cell value is not found in the map, the raw URL is shown as the link text instead. */
    linkMap?: {
        [x: string]: string;
    } | undefined;
};
type DateTableCell = {
    /** The type of the cell. */
    type: 'date';
    options?: {
        /** Valid values are time zone names in [IANA time zone database](https://www.iana.org/time-zones) */
        timeZone?: FormattedDateTimeZoneOption;
        /** Defaults to `short-year-month-day-time` when omitted.
        
        | style                                         | output                          |
        |-----------------------------------------------|---------------------------------|
        | `long-year`                                   | 1985                            |
        | `long-year-month`                             | October 1985                    |
        | `long-year-month-day`                         | October 25, 1985                |
        | `long-year-month-day-weekday`                 | Friday, October 25, 1985        |
        | `long-year-month-day-time`                    | October 25, 1985 at 8:21 PM     |
        | `long-year-month-day-time-timezone`           | October 25, 1985 at 8:21 PM ET  |
        | `long-month`                                  | October                         |
        | `long-month-day`                              | October 25                      |
        | `long-month-day-weekday`                      | Friday, October 25              |
        | `long-weekday`                                | Friday                          |
        | `medium-year-month`                           | Oct 1985                        |
        | `medium-year-month-day`                       | Oct 25, 1985                    |
        | `medium-year-month-day-weekday`               | Fri, Oct 25, 1985               |
        | `medium-year-month-day-time`                  | Oct 25, 1985, 8:21 PM           |
        | `medium-year-month-day-time-timezone`         | Oct 25, 1985, 8:21 PM ET        |
        | `medium-month`                                | Oct                             |
        | `medium-month-day`                            | Oct 25                          |
        | `medium-month-day-weekday`                    | Fri, Oct 25                     |
        | `medium-month-day-time`                       | Oct 25, 8:21 PM                 |
        | `medium-month-day-time-timezone`              | Oct 25, 8:21 PM ET              |
        | `short-year-month-day`                        | 10/25/85                        |
        | `short-year-month-day-time`                   | 10/25/85, 8:21 PM               |
        | `short-year-month-day-time-timezone`          | 10/25/85, 8:21 PM ET            |
        | `short-year-month-day-time-seconds`           | 10/25/85, 8:21:00 PM            |
        | `short-year-month-day-time-seconds-timezone`  | 10/25/85, 8:21:00 PM ET         |
        | `short-month-day`                             | 10/25                           |
        | `short-month-day-time`                        | 10/25, 8:21 PM                  |
        | `short-month-day-time-timezone`               | 10/25, 8:21 PM ET               |
        | `short-month-day-time-seconds`                | 10/25, 8:21:00 PM               |
        | `short-month-day-time-seconds-timezone`       | 10/25, 8:21:00 PM ET            |
        | `short-time`                                  | 8:21 PM                         |
        | `short-time-timezone`                         | 8:21 PM ET                      |
        | `short-time-seconds`                          | 8:21:00 PM                      |
        | `short-time-seconds-timezone`                 | 8:21:00 PM ET                   | */
        style?: FormattedDateStyleOption;
    } | undefined;
};
type FormattedDateTimeZoneOption = string | undefined;
type FormattedDateStyleOption = (StaticDateTimeStyle | DynamicDateTimeStyle) | undefined;
type StaticDateTimeStyle = 'long-year' | 'long-year-month' | 'long-year-month-day' | 'long-year-month-day-weekday' | 'long-year-month-day-time' | 'long-year-month-day-time-timezone' | 'long-month' | 'long-month-day' | 'long-month-day-weekday' | 'long-weekday' | 'medium-year-month' | 'medium-year-month-day' | 'medium-year-month-day-weekday' | 'medium-year-month-day-time' | 'medium-year-month-day-time-timezone' | 'medium-month' | 'medium-month-day' | 'medium-month-day-weekday' | 'medium-month-day-time' | 'medium-month-day-time-timezone' | 'short-year-month-day' | 'short-year-month-day-time' | 'short-year-month-day-time-timezone' | 'short-year-month-day-time-seconds' | 'short-year-month-day-time-seconds-timezone' | 'short-month-day' | 'short-month-day-time' | 'short-month-day-time-timezone' | 'short-month-day-time-seconds' | 'short-month-day-time-seconds-timezone' | 'short-time' | 'short-time-timezone' | 'short-time-seconds' | 'short-time-seconds-timezone';
type DynamicDateTimeStyle = 'long-year?-month-day' | 'long-year?-month-day-weekday' | 'long-year-month-day-time-timezone?' | 'medium-year?-month' | 'medium-year?-month-day' | 'medium-year?-month-day-weekday' | 'medium-year?-month-day-time' | 'medium-year?-month-day-time-timezone?' | 'short-year?-month-day' | 'short-year?-month-day-time' | 'short-year?-month-day-time-timezone?' | 'short-year?-month-day-time-seconds' | 'short-year?-month-day-time-seconds-timezone?' | 'short-time-timezone?' | 'short-time-seconds-timezone?';
type IdTableCell = {
    /** Renders the cell value as a Stripe object identifier (for example, `cus_abc123`) in a monospace font. Use this for columns that contain IDs such as customer IDs, charge IDs, or payment intent IDs. */
    type: 'id';
};
type StatusTableCell = {
    /** The type of the cell. */
    type: 'status';
    /** A map of cell values to status types. */
    statusMap: {
        [x: string]: 'neutral' | 'urgent' | 'warning' | 'negative' | 'positive' | 'info';
    };
};
type CurrencyTableCell = {
    /** The type of the cell. */
    type: 'currency';
    /** Three-letter ISO-4217 currency code or a non-ISO currency code like `USDC` for USD Coin or `BTC` for Bitcoin. */
    currency: CurrencyCode;
    /** @deprecated Use `currencyKey` instead. Specifies a per-item currency code override, keyed by table item id. If an item id is not present in the map, the value of currency is used. This allows a single column to display multiple currencies. */
    currencyMap?: {
        [x: string]: CurrencyCode;
    } | undefined;
    /** The key of the currency attribute in the table row item. If not provided, the currency falls back to currencyMap (keyed by table item id), and then to the default currency value. */
    currencyKey?: string | undefined;
};
export type CurrencyCode = string;
export type ListPageData = {
    items?: ListPageItem[] | undefined;
    /** Whether the data is loading. */
    loading?: boolean | undefined;
};
export type ListPageItem = {
    /** The unique identifier of the table item. */
    id: string;
} & {
    [x: string]: string | number;
};
export type ListPageEmptyMessage = {
    /** A custom message to display when the table is empty. */
    message: string;
    /** A call to action displayed below the message. */
    action: {
        /** The label of the action. */
        label: string;
        /** The link of the action. */
        href: string;
    };
};
type ListPagePagination = ControlledOffsetPaginationConfig | CursorPaginationConfig;
type ControlledOffsetPaginationConfig = {
    /** The number of rows displayed per page. */
    pageSize: number;
    /** The total number of items across all pages. Used to compute total page count and the "x–y of z results" caption. */
    totalItems: number;
    /** The current active page (1-indexed). The table reflects this value — the consumer is responsible for tracking and updating it. Clamped to the last page when it exceeds total pages. */
    currentPage: number;
    /** A callback invoked when the user navigates to a different page. Receives the target page number — the consumer is responsible for updating `currentPage`. */
    onPageChange: (pageNumber: number) => void;
};
type CursorPaginationConfig = {
    /** The number of rows displayed per page. */
    pageSize: number;
    /** The current active page (1-indexed). Required in cursor mode because the table cannot derive page state without a total count — the consumer must track this externally. */
    currentPage: number;
    /** A callback invoked when the user navigates to a different page. Required in cursor mode — the consumer is responsible for fetching the appropriate page of data and updating `currentPage`. */
    onPageChange: (pageNumber: number) => void;
    /** Signals whether additional pages exist beyond the current one. The presence of this prop is the discriminant that activates cursor mode. When `true`, the Next button is enabled; when `false`, it is disabled. The "x–y of z results" caption is hidden since the total count is unknown. Data slicing is disabled — only the current page's items should be passed. */
    hasMore: boolean;
};
export type ListPageProps = {
    columns: ListPageColumn[];
    data: ListPageData;
    /** The message to display when the list page is empty. */
    emptyMessage?: (string | ListPageEmptyMessage) | undefined;
    /** The actions to display in the list page header. */
    headerActions?: {
        /** The unique identifier for the action button. */
        id: string;
        /** The label of the action button. */
        label: string;
        /** An event handler for the action button. */
        onPress: () => void;
    }[] | undefined;
    /** Configuration for paginating table rows. Supports 2 modes: (1) Controlled offset — pass `pageSize`, `currentPage`, `totalItems`, and `onPageChange`; the table reflects the consumer-provided page and fires the callback on navigation. (2) Cursor — for data sources that use [manual](https://docs.stripe.com/pagination#manual-pagination) [cursor-based pagination](https://docs.stripe.com/api/pagination) and only expose a `has_more` boolean signal with no total count. This is a common pattern in many RESTful APIs, including Stripe's own. Pass `pageSize`, `currentPage`, `onPageChange`, and `hasMore`; the table does not slice data (consumer passes only the current page's items) and relies on `hasMore` to enable/disable the Next button. */
    pagination?: ListPagePagination | undefined;
    /** The title of the list page. */
    title?: string | undefined;
};
export declare const ListPage: ("ListPage" & {
    readonly type?: "ListPage" | undefined;
    readonly props?: ListPageProps | undefined;
    readonly children?: true | undefined;
} & React.ComponentClass<import("@remote-ui/react").ReactPropsFromRemoteComponentType<RemoteComponentType<"ListPage", ListPageProps, true>>, any>) | ("ListPage" & {
    readonly type?: "ListPage" | undefined;
    readonly props?: ListPageProps | undefined;
    readonly children?: true | undefined;
} & React.FunctionComponent<import("@remote-ui/react").ReactPropsFromRemoteComponentType<RemoteComponentType<"ListPage", ListPageProps, true>>>);
export type ListPage = RemoteComponentType<'ListPage', ListPageProps>;
export {};
