import { jsx as _jsx } from "react/jsx-runtime";
import { createRemoteReactComponent } from '@remote-ui/react';
import { SDK_VERSION } from '../version';
const withSdkProps = (Component) => {
    const wrappedComponentName = Component.displayName || Component.toString();
    const WithSdkProps = (props) => (_jsx(Component, { ...props, wrappedComponentName: wrappedComponentName, sdkVersion: SDK_VERSION, schemaVersion: "v9Next" }));
    WithSdkProps.wrappedComponentName = wrappedComponentName;
    return WithSdkProps;
};
const defineComponent = (name, fragmentProps, wrapWithSdkProps) => {
    const remoteComponent = createRemoteReactComponent(name, {
        fragmentProps,
    });
    if (!wrapWithSdkProps) {
        return remoteComponent;
    }
    return withSdkProps(remoteComponent);
};
export const BarChart = defineComponent('BarChart', [], true);
export const LineChart = defineComponent('LineChart', [], true);
export const MeterChart = defineComponent('MeterChart', [], true);
export const SparkLineChart = defineComponent('SparkLineChart', [], true);
export const Tab = defineComponent('Tab', [], true);
export const Tabs = defineComponent('Tabs', [], true);
