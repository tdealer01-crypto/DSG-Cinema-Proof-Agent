import { jsx as _jsx } from "react/jsx-runtime";
import { createRemoteReactComponent } from '@remote-ui/react';
import { SDK_VERSION } from '../version';
const withSdkProps = (Component) => {
    const wrappedComponentName = Component.displayName || Component.toString();
    const WithSdkProps = (props) => (_jsx(Component, { ...props, wrappedComponentName: wrappedComponentName, sdkVersion: SDK_VERSION, schemaVersion: "v9Experimental" }));
    WithSdkProps.wrappedComponentName = wrappedComponentName;
    return WithSdkProps;
};
const defineComponent = (name, fragmentProps, wrapWithSdkProps) => {
    const remoteComponent = createRemoteReactComponent(name, {
        fragmentProps,
    });
    if (!wrapWithSdkProps) {
        return remoteComponent;
    }
    return withSdkProps(remoteComponent);
};
export const ListPage = defineComponent('ListPage', [], true);
