{
  "$schema": "https://stripe.com/stripe-app.schema.json",
  "id": "pics.dsg.governance",
  "name": "DSG Governance Gate",
  "version": "2.7.1",
  "icon": "./icon.png",
  "distribution_type": "public",
  "sandbox_install_compatible": true,
  "stripe_api_access_type": "oauth",
  "allowed_redirect_uris": [
    "__CINEMA_API_BASE__/marketplace/stripe/callback"
  ],
  "permissions": [
    {
      "permission": "charge_read",
      "purpose": "Read the payment amount, currency, status, and Radar risk level shown in this payment detail view so DSG can calculate the verification decision."
    },
    {
      "permission": "payment_intent_read",
      "purpose": "Read the current PaymentIntent amount, currency, and status shown in this payment detail view so DSG can calculate the verification decision."
    }
  ],
  "ui_extension": {
    "views": [
      {
        "viewport": "stripe.dashboard.payment.detail",
        "component": "ChargeGate"
      }
    ],
    "content_security_policy": {
      "connect-src": [
        "__CINEMA_API_BASE__/stripe/evaluate"
      ],
      "purpose": "Send bounded Stripe transaction context to the Cinema verification backend. The app never sends a Z3 credential or accepts arbitrary solver programs."
    }
  },
  "constants": {
    "CINEMA_API_BASE": "__CINEMA_API_BASE__"
  }
}
